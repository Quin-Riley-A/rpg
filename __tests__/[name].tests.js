import [function] from 'source-path'

describe('[function name]', () => {
  test('should [do something]', () => {
    //code
    // const date = new Date('2022-10-01');
    //expect statements
    expect([value]).toEqual([othervalue]);
  });
});